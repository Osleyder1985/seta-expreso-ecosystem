# OIDC resource-server configuration
# Example only; do not commit production secrets.
OIDC_ISSUER=http://localhost:8081/realms/seta-expreso
OIDC_AUDIENCE=seta-expreso-api
OIDC_JWKS_COOLDOWN_MS=5000

# PostgreSQL/PostGIS persistence
DATABASE_URL=postgresql://postgres:change-me-local-only@localhost:5432/seta_expreso

# OpenAPI documentation is opt-in; keep disabled in production unless explicitly required.
OPENAPI_ENABLED=false
