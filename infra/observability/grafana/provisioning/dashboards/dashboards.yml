apiVersion: 1

providers:
  - name: seta-expreso
    orgId: 1
    folder: SETA EXPRESO
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
