# NestJS Backend PoC
PoC mínimo para comparación. La vertical CRUD es deliberadamente pequeña. La persistencia en memoria permite ejecutar el contrato HTTP sin bloquear la reconstrucción; la integración PostgreSQL/transaction es un criterio explícito pendiente del benchmark y no debe considerarse satisfecho por esta implementación inicial.
